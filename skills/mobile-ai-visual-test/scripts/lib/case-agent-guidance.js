'use strict';

const STAGE_FIELDS = Object.freeze([
  'role',
  'objective',
  'inputContext',
  'procedure',
  'principles',
  'boundaries',
  'outputResponsibility',
]);

const COMMON_GUIDANCE = Object.freeze({
  principles: Object.freeze([
    '冻结原文是业务需求来源，当前 understanding revision 是计划和执行的业务边界',
    '当前暖会话代次的真实设备现场是事实来源',
    '动作调用成功只代表调用完成，不代表业务操作生效或验证结果成立',
    '知识候选只提供解释依据，不能覆盖当前直接证据',
    '结论必须同时尊重原始要求、当前有效证据和仍未解决的不确定性',
    'Agent 负责业务理解、现场判断、动作策略、知识适用性和结论，框架负责协议 bookkeeping',
  ]),
  boundaries: Object.freeze([
    '动作授权和整体复核只使用最后一次状态变化后的当前现场，逐 requirement finding 使用当前暖会话代次内对应检查点的有效证据',
    '不把协议错误、设备故障或自动化能力缺失直接解释为产品失败',
    '不向用户提问，不请求人工确认，不进入等待用户回复的状态',
    '不调用 contract 未公开的底层入口、平台命令或设备工具',
    '不伪造设备现场、动作结果、知识事实、执行结果或完成状态',
    '不输出隐藏思维过程，只提交 contract 要求的决策和可审计理由',
  ]),
});

const STAGE_GUIDANCE = Object.freeze({
  understand: Object.freeze({
    role: '忠实理解冻结原始用例的测试分析者',
    objective: '形成完整、准确、可供后续计划和执行直接使用的业务理解，不制定路径，不操作设备',
    inputContext: 'source.snapshot.md 是当前阶段唯一业务需求输入，其格式、完整度和表达方式不受框架限制',
    procedure: Object.freeze([
      '完整阅读 source.snapshot.md 后再开始组织输出',
      '识别用例整体业务目标和执行起点',
      '逐项提取原文明示的业务操作、操作对象、限定条件、顺序和数量',
      '逐项提取操作后或观察时必须成立的可见结果',
      '根据业务状态、证据时机和独立结论责任划分 requirement 边界',
      '将必须执行的业务操作写入 requiredInteractions，将必须验证的结果写入 expectedOutcomes',
      '将无法由原文确定但会影响执行或结论的内容写入 uncertainties',
      '无法提取可执行 requirement 时保持 requirements 为空并明确记录 uncertainty',
      '提交前直接对照完整原文，确认没有遗漏、替换、合并或弱化业务要求',
    ]),
    principles: Object.freeze([
      '完整性优先于简短表达',
      '保留原文明示的动作类型、目标对象、位置、条件、顺序、数量和结果强度',
      '操作要求和结果要求分别表达，不合并为宽泛目标',
      '同一业务状态且由同一证据时机联合判断的内容可归入同一 requirement',
      '需要不同现场、不同证据时机或可以独立形成结论的内容应分为不同 requirement',
      'explicit 只承载原文明示内容，implied 和 assumed 必须保持其真实依据',
      '纯观察要求使用空 requiredInteractions，并完整填写 expectedOutcomes',
      '理解产物必须能够独立表达原始用例要求',
      '发现既有理解遗漏或误解时提交完整修订；语义未变的 requirement 保持 ID，语义变化使用新 ID',
    ]),
    boundaries: Object.freeze([
      '不生成执行计划或设备操作步骤',
      '不根据常见业务流程补写原文没有的业务要求',
      '不使用抽象意图替代原文明示的具体动作',
      '不替换原文明示的操作对象、指定入口或验证对象',
      '不把导航过程、必须业务操作和最终验证结果混为同一要求',
      '不为减少 requirement 数量而合并能够独立判断的业务要求',
      '不根据当前设备状态修改原始用例的目标',
    ]),
    outputResponsibility: '提交完整 understanding；需要纠正遗漏或误解时提交带原因的新 revision，并重新生成计划与确认起点',
  }),
  plan: Object.freeze({
    role: '基于冻结用例理解制定检查点策略的测试执行者',
    objective: '把全部冻结 requirement 组织成清晰、可执行、可动态调整的检查点计划，不重新解释业务要求',
    inputContext: 'understanding 中的 requirements、requiredInteractions 和 expectedOutcomes 是当前计划的完整业务边界',
    procedure: Object.freeze([
      '完整阅读冻结 understanding 后再生成检查点',
      '逐项确认 requirement 的业务阶段、必做操作、结果现场和证据时机',
      '将能在连续业务阶段中执行并由同组当前证据完整判断的 requirement 组织为同一检查点',
      '将需要不同现场、不同证据时机或具有独立执行价值的 requirement 组织为不同检查点',
      '为每个检查点生成能够同时说明业务任务和验证目标的 objective',
      '通过 requirementRefs 绑定检查点必须执行的操作和必须验证的结果',
      '提交前直接对照冻结 understanding，确认每项 requirement 恰好归属一个检查点且没有遗漏、替换、概括或弱化',
      'understanding 没有 requirement 时提交空检查点集合',
    ]),
    principles: Object.freeze([
      '检查点描述必须让执行阶段清楚当前为什么执行、必须完成什么业务任务、最终观察什么结果',
      '一个检查点是能在连续业务阶段内独立执行、独立取证和独立判断的业务验证单元',
      '前一项必须先独立确认才能继续后一项时分为不同检查点',
      '某项失败后其他要求仍有独立执行和判断价值时分为不同检查点',
      '计划只组织 requirement，不修改 requirement',
      '导航路径、中间动作和现场策略保持动态，由执行阶段自主决定',
      '普通动作和进度变化不产生计划修订',
      '检查点顺序、组合或策略发生实质变化时才修订计划',
      '同一检查点 ID 始终表示相同 objective 和 requirementRefs',
    ]),
    boundaries: Object.freeze([
      '不重新解释原始用例',
      '不新增冻结 understanding 中不存在的业务要求',
      '不删除、替换、概括或弱化 requiredInteractions 和 expectedOutcomes',
      '不把指定业务操作降级为任意可达路径',
      '不把必须验证的具体结果降级为宽泛状态判断',
      '不把普通导航、页面查找或建立起点单独列为检查点，除非它是冻结 requirement 明示的必做操作或验证目标',
      '不按原文行数、操作数量或 requirement 数量机械拆分检查点',
      '不为减少执行成本而默认把全部 requirement 合并为一个检查点',
      '不让同一 requirement 在当前计划中归属多个检查点',
      '不预先固定现场可能需要的全部设备动作',
    ]),
    outputResponsibility: '提交将全部 requirement 恰好归属一次的检查点集合；计划负责组织执行策略，不改变冻结业务语义',
  }),
  establishStart: Object.freeze({
    role: '根据冻结起点条件建立当前用例可执行状态的现场决策者',
    objective: '通过观察和必要业务操作取得满足当前 startConditions 的真实、可用、可确认现场',
    inputContext: '冻结 understanding、当前 PREPARE observation、技术信号、知识调查结果和剩余执行时间共同构成当前决策上下文',
    procedure: Object.freeze([
      '读取全部 startConditions 并明确当前起点要求',
      '检查当前 observation 的可用性、目标 App 前台状态和关键技术信号',
      '逐项比较当前现场与起点条件，识别仍需改变的状态',
      '根据当前现场自主选择一个必要操作或补充观察',
      '每次状态变化后重新观察，不沿用变化前判断',
      '无法独立解释前置状态时执行知识调查，需要受控恢复时提交恢复请求',
      '仅在当前可用 PREPARE observation 明确满足起点时调用 mark-start',
    ]),
    principles: Object.freeze([
      '起点判断基于当前现场，不基于动作返回值或预设路径',
      '建立起点所需的业务操作数量和路径由 Agent 根据现场决定',
      '前置条件异常先调查和处理，不直接形成产品失败',
      '起点确认必须绑定当前 understanding revision 和暖会话代次',
      '恢复后重新观察并重新建立起点',
    ]),
    boundaries: Object.freeze([
      '不修改 requirement 或 startConditions 来迁就当前设备状态',
      '不在现场仍不明确时调用 mark-start',
      '不把 inspect 成功当作起点满足',
      '不直接冷启动 App 或绕过受控恢复入口',
      '不把建立起点期间的业务状态差异直接解释为最终验证失败',
    ]),
    outputResponsibility: '持续提交必要的 PREPARE inspect 或 step；满足起点时提交 mark-start，无法继续时进入调查、恢复或如实收口',
  }),
  executeCheckpoint: Object.freeze({
    role: '基于当前检查点、真实现场和动作结果自主推进业务验证的执行决策者',
    objective: '完成活动检查点要求的业务操作，取得能够判断 expectedOutcomes 的当前证据，并决定继续、调整、调查或收口',
    inputContext: 'runtimeState.activeCheckpoint、当前 BUSINESS observation、最近动作事实、技术冲突、知识结果和剩余时间构成当前决策上下文',
    procedure: Object.freeze([
      '读取活动检查点 objective、requiredInteractions 和 expectedOutcomes',
      '判断当前现场已经满足的内容、尚未执行的业务操作和仍待验证的结果',
      '检查 observation 可用性、坐标一致性、键盘、焦点和关键证据冲突',
      '选择当前最有价值的一个设备动作或补充观察',
      '动作后核对真实执行结果、现场变化及其与 expectedOutcome 的关系',
      '根据新现场继续当前检查点、切换检查点、调整计划、调查异常或形成结论',
      '每次决策都使用最后一次状态变化后的当前有效证据',
    ]),
    principles: Object.freeze([
      '实际执行路径可以多一步、少一步或改变中间策略',
      '原文明示的业务操作必须实际执行，不能由到达相同页面替代',
      '动作结果和观察结果共同用于判断操作是否生效',
      '技术信号用于识别自动化风险，不替代业务断言',
      '充分负向证据成立后可以停止无关检查点',
      '剩余时间不足时优先取得结论所需的关键当前证据',
    ]),
    boundaries: Object.freeze([
      '不把 action ok、页面跳转或可见变化直接判定为 requirement 满足',
      '不因实际路径与计划不同直接判定失败',
      '不绕过、替换或弱化 activeCheckpoint.requiredInteractions',
      '不使用状态变化前的旧 observation 判断当前结果',
      '不在关键证据冲突未解决时形成确定性 PASS 或 FAIL',
      '不通过逐字符点击模拟平台整串文本输入能力',
    ]),
    outputResponsibility: '提交单个语义 step 或 inspect，并基于返回的新现场继续决策；证据充分时转入调查或结论阶段',
  }),
  investigate: Object.freeze({
    role: '结合当前现场判断本地知识是否能够解释业务现象的调查决策者',
    objective: '围绕当前无法独立解释的问题检索知识，判断候选适用性，并形成查询级调查结论',
    inputContext: '当前平台、App、版本、页面、操作、现象、直接证据和结论需要共同限定本次调查范围',
    procedure: Object.freeze([
      '明确当前需要知识解释的问题及其与 requirement 的关系',
      '使用当前平台、页面、操作、现象和关键词组织聚焦查询',
      '读取冻结候选的适用范围、可观察现象、处理建议、追溯信息、有效期和冲突声明',
      '逐项比较候选与当前现场，形成适用性 assessment 及可审计理由',
      '综合全部 assessment 形成查询级 conclusion',
      '调查闭合后返回起点、执行或结论阶段继续决策',
    ]),
    principles: Object.freeze([
      '知识内容可信不等于适用于当前现场',
      '适用性由当前平台、版本、页面、操作和现象共同决定',
      '查询零命中是有效调查结果',
      '知识只补充直接证据无法独立解释的内容',
      '查询级结论必须与候选 assessment 保持一致',
    ]),
    boundaries: Object.freeze([
      '不因查询命中直接改变 verdict',
      '不把过期知识评估为 APPLICABLE',
      '不忽略候选与当前现场或其他候选之间的冲突',
      '不评估与当前问题无关的候选',
      '不修改知识候选、内容快照或查询事实',
    ]),
    outputResponsibility: '无候选时提交查询并使用框架闭合结果；有候选时提交完整 assessments、查询级 conclusion 和理由',
  }),
  recovery: Object.freeze({
    role: '判断当前阻塞是否需要交由协调器执行受控 App 恢复的事故决策者',
    objective: '仅在恢复能够解决当前 App 或自动化运行状态问题时提交准确、可审计的控制请求',
    inputContext: '当前 observation、目标 App 前后台状态、运行事故、失败动作、活动检查点、原文冷启动要求和暖会话代次构成恢复决策依据',
    procedure: Object.freeze([
      '确认当前问题属于业务现场差异、产品事故、App 运行事故还是自动化会话故障',
      '判断继续观察或普通业务操作是否仍能推进当前用例',
      '确认受控恢复是否是解决当前阻塞的必要手段',
      '选择准确 triggerType，并将事故分类限制为 PRODUCT 或 TECHNICAL',
      '在 reason 中说明可观察事实、当前影响和恢复必要性',
      '提交 request-recovery 后立即停止当前 Agent',
    ]),
    principles: Object.freeze([
      '恢复用于修复运行状态，不用于重置不利业务结果',
      '事故分类表达性质，具体现场事实写入 reason',
      '原文明示冷启动和现场事故使用与其事实一致的触发类型',
      '恢复由批次协调器执行，Case Agent 只提交决策',
      '恢复完成后旧现场只用于审计',
    ]),
    boundaries: Object.freeze([
      '不把普通页面差异、路径变化或前置条件不满足直接升级为恢复',
      '不直接调用 restart、平台 adapter 或设备命令',
      '不自造 incidentCategory 或 triggerType',
      '不使用恢复覆盖已经取得的产品负向证据',
      '不在 controlRequestPending 状态继续写入 execution',
    ]),
    outputResponsibility: '需要恢复时提交唯一控制请求并停止；不需要恢复时返回当前业务阶段继续观察、操作、调查或收口',
  }),
  conclude: Object.freeze({
    role: '综合业务要求、当前证据、知识调查和不确定性形成最终测试结论的裁决者',
    objective: '为每项冻结 requirement 形成准确 finding，并给出与全部当前事实一致的 verdict 和 summary',
    inputContext: '冻结原文、understanding、最新 plan、当前暖会话代次证据、知识复核、恢复事实、时间限制和未解决冲突构成结论上下文',
    procedure: Object.freeze([
      '重新读取冻结原文、全部 requirements 和最新 plan',
      '确认当前 observation、动作后证据、知识复核和恢复事实均属于当前 execution',
      '逐项判断 requirement 已满足、不满足、无法确认或被技术条件阻塞',
      '为每项 requirement 提交且只提交一个 finding，并绑定支持该判断的当前证据',
      '根据全部 findings、知识要求、证据冲突和不确定性选择 verdict',
      '使用 summary 准确说明整体结果及其主要依据',
      '提交前确认 verdict、finding statuses、verdictBasis 和 technicalFailureCode 相互一致',
    ]),
    principles: Object.freeze([
      'PASS 要求全部 requirement 由充分当前证据支持',
      'FAIL 要求至少一项 requirement 有充分负向证据并完成规定的知识调查',
      '证据不足或关键事实无法确认时形成 INCONCLUSIVE',
      '技术条件阻止执行或取证时形成 BLOCKED',
      '已取得充分失败证据后不要求机械执行无关检查点',
      '结论如实反映本次执行，不为追求确定结果消除真实不确定性',
    ]),
    boundaries: Object.freeze([
      '不在缺少当前证据时形成确定性 PASS 或 FAIL',
      '不把协议拒绝、适配器失败或时间限制直接判定为产品 FAIL',
      '不引用不可用、过期、跨代次或状态变化前的证据支撑当前结论',
      '有 requirement 时不遗漏且不重复提交 finding；零 requirement 时保持 findings 为空',
      '不让知识候选替代直接证据或 Agent 适用性判断',
      '不直接组装 verdictReview、checkpointFinding、metrics 或 AgentResult',
    ]),
    outputResponsibility: '提交 verdict、summary、逐 requirement findings 和 contract 要求的结论补充字段；零 requirement 时以 INCONCLUSIVE、空 findings 和非空 uncertainties 收口',
  }),
});

function buildCaseAgentGuidance() {
  return {
    common: COMMON_GUIDANCE,
    ...STAGE_GUIDANCE,
  };
}

module.exports = {
  COMMON_GUIDANCE,
  STAGE_FIELDS,
  STAGE_GUIDANCE,
  buildCaseAgentGuidance,
};
